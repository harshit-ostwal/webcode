const API_VERSION = "v1";
const API_PREFIX = `/api/${API_VERSION}`;

const REQUEST_SIZE_LIMIT = "16kb";
const MAX_DEVICE_LIMIT = 5;

export { API_PREFIX, API_VERSION, MAX_DEVICE_LIMIT, REQUEST_SIZE_LIMIT };
